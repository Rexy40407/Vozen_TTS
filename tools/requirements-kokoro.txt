# tools/requirements-kokoro.txt — deps do sidecar TTS Kokoro (kokoro-onnx, ONNX/CPU).
# Instalado por setup-kokoro.ps1 em tools/kokoro-venv/ (gitignored). Versões PINADAS
# para resolução determinística (falha alto se um wheel desaparecer). SEM PyTorch/GPU
# — corre em CPU. O espeak-ng vem EMPACOTADO via espeakng-loader, por isso NÃO é
# preciso instalar espeak-ng no sistema (necessário para o g2p de pt/es/fr/…).
kokoro-onnx==0.5.0
soundfile==0.14.0
onnxruntime==1.27.0
# NumPy 2.5.x is not published for Python 3.11 (the Debian Bookworm
# runtime); 2.3.5 is the newest compatible release and works with
# onnxruntime 1.27 on CPU.
numpy==2.3.5
phonemizer-fork==3.3.2
espeakng-loader==0.2.4
