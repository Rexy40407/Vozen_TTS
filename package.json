{
  "name": "vozen",
  "version": "0.0.0",
  "private": true,
  "description": "Vozen — type it, hear it. Free neural TTS bot for Discord (Piper, self-host) with reliability.",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "start:prod": "npm run build && node scripts/start-prod.mjs",
    "start:sharded": "node dist/shard.js",
    "backup:db": "node scripts/backup-db.mjs",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run build && npm run typecheck && npm run lint && npm run format:check && npm run check:rust-contracts && npm run check:rust-canaries && npm run check:rust && vitest run",
    "register": "tsx src/bot/registerCommands.ts",
    "build:site": "node tools/minify-site.mjs",
    "build:rust-contracts": "npm run build:rust-command-contract && npm run build:rust-schema-contract && npm run build:rust-voice-i18n && npm run build:rust-voice-display && npm run build:rust-game-content && npm run build:rust-game-catalog",
    "build:rust-command-contract": "tsx tools/export-rust-contracts.ts",
    "build:rust-schema-contract": "tsx tools/export-rust-schema.ts",
    "check:rust-contracts": "npm run check:rust-command-contract && npm run check:rust-schema-contract && npm run check:rust-voice-i18n && npm run check:rust-voice-display && npm run check:rust-game-content && npm run check:rust-game-catalog",
    "check:rust-command-contract": "tsx tools/export-rust-contracts.ts --check",
    "check:rust-schema-contract": "tsx tools/export-rust-schema.ts --check",
    "build:rust-voice-i18n": "tsx tools/export-rust-voice-i18n.ts",
    "check:rust-voice-i18n": "tsx tools/export-rust-voice-i18n.ts --check",
    "build:rust-voice-display": "tsx tools/export-rust-voice-display.ts",
    "check:rust-voice-display": "tsx tools/export-rust-voice-display.ts --check",
    "build:rust-game-content": "tsx tools/export-rust-game-content.ts",
    "check:rust-game-content": "tsx tools/export-rust-game-content.ts --check",
    "build:rust-game-catalog": "tsx tools/export-rust-game-catalog.ts",
    "check:rust-game-catalog": "tsx tools/export-rust-game-catalog.ts --check",
    "check:rust-canaries": "node tools/check-rust-canaries.mjs",
    "check:rust": "cargo fmt --check && cargo check --workspace --all-targets --locked && cargo clippy --workspace --all-targets --locked -- -D warnings && cargo test --workspace --locked",
    "check:rust-voice": "cargo check -p vozen-runtime --features voice-driver --all-targets --locked && cargo test -p vozen-runtime --features voice-driver --locked && cargo clippy -p vozen-runtime --features voice-driver --all-targets --locked -- -D warnings",
    "rust:staging:preflight": "tsx tools/rust-staging-preflight.ts",
    "build:i18n": "node tools/build-i18n.mjs",
    "check:i18n": "node tools/build-i18n.mjs --check",
    "check:site-copy": "node tools/check-site-copy.mjs",
    "check:site": "vitest run tests/operationalHardening.test.ts tests/siteTrust.test.ts tests/siteI18n.test.ts tests/dashboardCoreSettings.test.ts tests/siteUxPolish.test.ts tests/fullAuditRegression.test.ts && npm run check:i18n && npm run check:site-copy && npm run build:site"
  },
  "dependencies": {
    "@discordjs/opus": "^0.10.0",
    "@discordjs/voice": "^0.19.2",
    "better-sqlite3": "^11.8.1",
    "chess.js": "^1.4.0",
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.7",
    "ffmpeg-static": "^5.2.0",
    "franc": "^5.0.0",
    "prism-media": "^1.3.5",
    "sodium-native": "^4.3.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.13.1",
    "@vitest/coverage-v8": "^3.2.6",
    "clean-css": "^5.3.3",
    "eslint": "^10.6.0",
    "eslint-config-prettier": "^10.1.8",
    "html-minifier-terser": "^7.2.0",
    "parse5": "^8.0.1",
    "prettier": "^3.9.4",
    "terser": "^5.48.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.63.0",
    "vitest": "^3.0.5"
  },
  "//overrides": "Transitive CVE fixes that stay within the same major version. NEVER run `npm audit fix --force`: it would downgrade discord.js to v13 and move @discordjs/opus across a major version. undici<=6.26.0: four high-severity advisories through @discordjs/rest, pinned to 6.27.0. trim<0.0.3: ReDoS through franc/trigram-utils on user-provided language-detection text, pinned to 0.0.3 with the same API. tar<=7.5.15: install-time path traversal through @discordjs/opus/node-pre-gyp, pinned to 7.5.19. Accepted low practical risk: the @discordjs/opus decode DoS advisory does not affect Vozen's trusted encode-only path.",
  "overrides": {
    "undici": "^6.27.0",
    "trim": "0.0.3",
    "tar": "^7.5.19"
  },
  "//scripts-policy": "The old `allowScripts` key was inert because no installed tool consumed it, so `npm ci` executed every dependency install script in CI and deployment. The workflows now use `npm ci --ignore-scripts` followed by explicit rebuilds for the four packages that require their own install step: better-sqlite3, @discordjs/opus, esbuild, and ffmpeg-static. Add any future native dependency to the explicit rebuild list in both workflows."
}
