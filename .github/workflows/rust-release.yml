name: Rust Vozen release

on:
  workflow_dispatch:
  push:
    branches: [feature/ecosystem-entitlements-api]

permissions:
  contents: read

concurrency:
  group: vozen-rust-release
  cancel-in-progress: true

jobs:
  linux-release:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.97.1
        with:
          components: rustfmt, clippy
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Check entitlement daemon dependency surface
        shell: bash
        run: |
          cargo tree -p vozen-entitlementd --no-default-features --edges normal > entitlementd-deps.txt
          if grep -E '(^|[[:space:]])(failure|failure_derive|chess|serenity|songbird|libcrux-[^[:space:]]*|rustls-webpki v0\.102\.)' entitlementd-deps.txt; then
            echo "forbidden dependency reached the entitlement daemon graph"
            exit 1
          fi
      - name: Test workspace
        run: cargo test --workspace
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Build runtime
        run: cargo build --release -p vozen-runtime
      - name: Build entitlement daemon
        run: cargo build --release -p vozen-entitlementd
      - name: Package
        run: |
          mkdir -p release/bin release/deploy
          cp target/release/vozen-runtime release/bin/vozen-runtime
          cp target/release/vozen-entitlementd release/bin/vozen-entitlementd
          cp deploy/vozen-rust.service release/deploy/
          cp deploy/vozen-entitlementd.service release/deploy/
          cp deploy/RUST-CUTOVER.md release/deploy/
          tar -czf vozen-rust-${GITHUB_SHA}.tar.gz -C release .
      - uses: actions/upload-artifact@v4
        with:
          name: vozen-rust-linux-${{ github.sha }}
          path: vozen-rust-${{ github.sha }}.tar.gz
          if-no-files-found: error
